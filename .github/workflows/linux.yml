name: Linux

on:
  workflow_dispatch:
  push:
    paths:
      - '**'
      - 'examples/**'
      - '.github/workflows/linux.yml'
    branches: [ "main", "dev" ]
  pull_request:
    paths:
      - '**'
      - 'examples/**'
      - '.github/workflows/linux.yml'
    branches: [ "main", "dev" ]
  release:
    types: [published]

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: write  # for actions/upload-release-asset to upload release asset
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        bits: [64]
        include:
        - bits: 64
          ARCH: "x86_64"
          ARCH_NAME: "amd64"
          COMPILER_PATH: "/user/bin"

    env:
      RELEASE_NAME: sili-dev_linux_${{ matrix.ARCH_NAME }}

    steps:
    - name: Checkout code
      uses: actions/checkout@master

    - name: Setup Release Version
      run: |
        echo "RELEASE_NAME=sili-${{ github.event.release.tag_name }}_linux_${{ matrix.ARCH_NAME }}" >> $GITHUB_ENV
      shell: bash
      if: github.event_name == 'release' && github.event.action == 'published'

    - name: Setup Environment
      run: |
        sudo apt-get update -qq
        sudo apt-get install -y software-properties-common
        sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
        sudo apt-get update
        sudo apt-get install -y gcc-15 g++-15 gcc-15-multilib g++-15-multilib

        sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-15 150 \
                                   --slave /usr/bin/g++ g++ /usr/bin/g++-15

        sudo apt-get install -y --no-install-recommends \
          libx11-dev mesa-common-dev libglx-dev libxcursor-dev \
          mesa-vulkan-drivers libxrandr-dev libxinerama-dev \
          libxi-dev libxext-dev libxfixes-dev libwayland-dev \
          libxkbcommon-dev libgles2-mesa-dev libasound2-dev \
          libvulkan-dev
            
        mkdir build
        cd build
        mkdir ${{ env.RELEASE_NAME }}
        cd ${{ env.RELEASE_NAME }}
        mkdir include
        mkdir lib
        cd ../../../sili
        # ${{ matrix.ARCH }}-linux-gnu-gcc -v

    - name: Tests
      run: |
        make compile_tests CC=gcc MODE=DEBUG

    - name: Examples
      run: |
        make compile_examples CC=gcc MODE=DEBUG

    - name: Build Library
      run: |
        make static CC=gcc MODE=RELEASE
        make dynamic CC=gcc MODE=RELEASE
      if: matrix.bits == 64

    - name: Generate Artifacts
      run: |
        cp -v ./sili.h ./build/${{ env.RELEASE_NAME }}/include
        cp -v ./build/libsili.a ./build/${{ env.RELEASE_NAME }}/lib
        cp -v ./build/libsili.so ./build/${{ env.RELEASE_NAME }}/lib
        cp -v ./README.md ./build/${{ env.RELEASE_NAME }}/README.md
        cp -v ./LICENSE ./build/${{ env.RELEASE_NAME }}/LICENSE
        cd build
        tar -czvf ${{ env.RELEASE_NAME }}.tar.gz ${{ env.RELEASE_NAME }}

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.RELEASE_NAME }}.tar.gz
        path: ./build/${{ env.RELEASE_NAME }}.tar.gz

    - name: Upload Artifact to Release
      uses: softprops/action-gh-release@v1
      with:
        files: ./build/${{ env.RELEASE_NAME }}.tar.gz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: github.event_name == 'release' && github.event.action == 'published'
