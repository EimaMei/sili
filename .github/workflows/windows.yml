name: windows

on:
	push:
		branches: [ "main", "dev" ]
	pull_request:
		branches: [ "main", "dev" ]

jobs:
	build:

	runs-on: windows-latest

	steps:
		- uses: actions/checkout@v3
		- name: make
			run: make run_examples